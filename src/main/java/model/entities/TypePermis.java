package model.entities;

public enum TypePermis {
    B,   // vehicule leger
    C,   // poids lourd
    D,   // transport en commun
    EC,  // remorque + poids lourd
    ED   // remorque + transport en commun
}
